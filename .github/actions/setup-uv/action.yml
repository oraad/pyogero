name: Setup uv
description: Install uv and sync frozen dev dependencies for pyogero

runs:
  using: composite
  steps:
    - uses: astral-sh/setup-uv@v6
      with:
        python-version: "3.14"
        enable-cache: true
    - run: uv sync --frozen --group dev
      shell: bash
